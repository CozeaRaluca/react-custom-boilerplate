import React from 'react';
import { render } from '@testing-library/react';

import {{properCase name}} from './{{properCase name}}';

test('{{ properCase name }} is in document', () => {
  const { queryByTestId } = render(<{{properCase name}} />);

  expect(queryByTestId('{{dashCase name}}')).toBeInTheDocument();
});
