import { SET_DATA } from './constants';

function {{camelCase name}}Reducer(state, action) {
  switch (action.type) {
    case SET_DATA:
      return {
        ...state,
        data: action.data,
      };
    default: {
      throw new Error(`Unhandled action type: ${action.type}`)
    }
  }
}

export default {{camelCase name}}Reducer;
