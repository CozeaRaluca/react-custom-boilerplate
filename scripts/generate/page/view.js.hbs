import React from 'react';
import PropTypes from 'prop-types';

const {{properCase name}} = (props) => {
  return (
    <div
      className="{{dashCase name}}-page"
      data-testid="{{dashCase name}}-page"
    />
  );
};

{{properCase name}}.propTypes = {};

export default {{properCase name}};
